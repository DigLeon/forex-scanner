# v5.3.4.4 — WAIT/TRADE score > 50 research logging

Research logging is now aligned with the GUI decision stream:

- log only `WAIT` or `TRADE`;
- require `score > 50` (strictly greater than 50);
- do not reject a record only because market data is marked `STALE`; freshness is preserved as metadata for later analysis;
- keep expiration and entry/reference price requirements so the outcome can be measured;
- keep deduplication by setup + decision; a WAIT record does not block a later TRADE record for the same setup;
- Telegram and trading/entry logic are unchanged.

This provides an end-of-day research sample of all eligible GUI WAIT/TRADE signals with score above 50.
