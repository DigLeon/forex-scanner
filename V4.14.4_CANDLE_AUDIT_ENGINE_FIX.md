# v4.14.4 — Candle Audit Engine Fix

- Fixes Candle Audit `N/A`: confirmation engine now receives real closed candle arrays instead of SMC timeframe-analysis objects.
- 1M uses closed REST candles directly.
- 3M and 5M are aggregated locally from closed 1M candles.
- A partial trailing 3M/5M bucket is removed so confirmation uses only fully closed candles.
- 5M remains diagnostic-only and does not alter the final decision.
- Score, Entry Zone, Signal Strength, TOO LATE/WORST ENTRY, Telegram gating, and WebSocket behavior are unchanged.
