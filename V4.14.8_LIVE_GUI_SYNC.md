# v4.14.8 — Live GUI Sync for Smart REST Recheck

- Browser polls only the local `/api/fast-recheck` endpoint every 2.5 seconds.
- No new Twelve Data calls are created by GUI polling.
- WAIT rows update automatically without pressing SCAN again.
- SKIP removes the row because SKIP is hidden in Scan Decisions.
- READY is shown as READY and remains observational; it does not bypass the authoritative full-scan TRADE/Telegram gate.
- Server keeps recent terminal recheck states for 5 minutes so the GUI can observe READY/SKIP even after the market watch is removed.
- WebSocket behavior is unchanged.
