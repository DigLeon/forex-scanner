# v5.3.4.18 — Paper Win Rate by Expiration GUI fix

- Fixes v5.3.4.17 packaging/UI issue: the server serves `public/index.html`, while the new expiration panel had only been added to the root `index.html`.
- Adds the visible `Paper Win Rate by Expiration` panel to `public/index.html` on the Auto Scanner page.
- Loads values from `/api/signal-stats` and `stats.byExpiration` on GUI refresh/server startup.
- Groups dynamically by actual `expirationMinutes`; no hard-coded horizon list.
- Displays `Xm · win rate · W/L/F` for each available expiration.
- Overall Paper Win Rate remains unchanged.
- No changes to Score Engine, Entry Engine, Expiration Engine, Telegram, or signal-generation logic.
