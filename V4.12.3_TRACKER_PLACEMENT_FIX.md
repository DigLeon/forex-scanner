# v4.12.3 Tracker Placement Fix

Persistent zone tracking now runs immediately after `combinedAnalysis()` and
before score diagnostics and all later WAIT/SKIP/OPPORTUNITY/CANDIDATE/TOO LATE
branches.

Result: every active `entryZoneLifecycle` is offered to `trackEntryZone()`,
including confirmed signals that previously exited before reaching the tracker.

Structural FVG identity from v4.12.2 is preserved.
No score thresholds, hard entry blockers, TRADE logic, or Telegram logic changed.
